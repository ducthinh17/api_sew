fastapi
uvicorn
gunicorn # Render sử dụng Gunicorn để chạy ứng dụng Python của bạn

# PyTorch và Torchvision phiên bản CPU để giảm kích thước và tương thích
# Sử dụng --find-links hoặc --index-url để trỏ đến wheel CPU
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu

Pillow
numpy
scikit-learn
pillow-heif # LƯU Ý: Thư viện này có thể cần native dependencies.
            # Nếu build trên Render lỗi, bạn có thể cần dùng Docker deployment
            # để cài đặt libheif-dev và libde265-dev.
